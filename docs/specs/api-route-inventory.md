# Agentic API Route Inventory

This inventory is the canonical map for `apps/web/app/api/**/route.ts`. Every route handler must either appear here or be removed. Stable public routes can be documented in user-facing guides, while authenticated UI/internal and preview routes are implementation contracts for the Agentic dashboard and worker loop.

Feature capability contracts in `apps/web/lib/feature-capabilities.ts` are validated against the same route exports. A capability may only advertise HTTP methods that the corresponding `route.ts` actually exports, and newly exported methods on tracked capability routes must be reflected in the registry before the capability smoke report can pass.

## Classification Policy

| Classification | Meaning |
| --- | --- |
| Public operational | Unauthenticated liveness, readiness, or session bootstrap surface. |
| Public signed | Public only when the caller presents a bounded signed token or equivalent proof. |
| Authenticated UI/internal | Dashboard-owned JSON contract that requires an Agentic session or access key. |
| Admin/governance | Authenticated route that mutates policy, workspace, privacy, or recovery state. |
| Integration boundary | OAuth callback, provider webhook, notification, or external event ingestion route. |
| Worker/job | Poll, replay, or stream route for durable execution jobs. |
| Preview/internal | Experimental or dashboard-only surface that should not be treated as a stable public API. |

New route handlers must update this file in the same change unless they are deleted before merge. If a route is stable/public, also update `docs/specs/agentic.md` or the relevant runbook with status, auth, and failure behavior.

## Route Inventory

| Endpoint | Methods | Classification | Auth | Stability | Owner/module | Purpose |
| --- | --- | --- | --- | --- | --- | --- |
| `/api/agents` | `GET, POST` | Authenticated UI/internal | Session or access key | Preview | Agents | List and create custom agent definitions. |
| `/api/agents/:id` | `GET, PUT, DELETE` | Authenticated UI/internal | Session or access key | Preview | Agents | Inspect, update, or delete one custom agent. |
| `/api/agents/:id/clone` | `POST` | Authenticated UI/internal | Session or access key | Preview | Agents | Clone an agent definition for customization. |
| `/api/agents/:id/export` | `GET` | Authenticated UI/internal | Session or access key | Preview | Agents | Export one agent definition. |
| `/api/agents/:id/memories` | `GET, POST` | Authenticated UI/internal | Session or access key | Preview | Agents and memory | Inspect or add agent-scoped memory. |
| `/api/agents/:id/metrics` | `GET` | Authenticated UI/internal | Session or access key | Preview | Agents | Load agent performance and usage metrics. |
| `/api/agents/activity` | `GET` | Authenticated UI/internal | Session or access key | Preview | Agents | Load recent agent activity. |
| `/api/agents/import` | `POST` | Authenticated UI/internal | Session or access key | Preview | Agents | Import a custom agent definition. |
| `/api/approvals/:id/respond` | `POST` | Authenticated UI/internal | Session or access key | Stable | Approvals and policy | Record an approval or rejection for a gated action. |
| `/api/approvals/batch/preview` | `POST` | Authenticated UI/internal | Session or access key | Preview | Approvals | Preview a batch approval operation. |
| `/api/approvals/batch/respond` | `POST` | Authenticated UI/internal | Session or access key | Preview | Approvals | Apply a bounded batch approval decision. |
| `/api/approvals/jobs/:id` | `GET` | Worker/job | Session or access key | Stable | Worker runtime | Poll approval follow-up or notification job status. |
| `/api/autopilot/events` | `POST` | Admin/governance | Session or access key | Stable | Autopilot | Queue deduplicated watcher, template, briefing, or connector events. |
| `/api/autopilot/settings` | `GET, POST` | Admin/governance | Session or access key | Stable | Autopilot | Inspect or update autopilot mode and scheduling controls. |
| `/api/briefing` | `POST` | Authenticated UI/internal | Session or access key | Stable | Briefings | Enqueue a startup, midday, meeting, end-of-day, or next-day briefing. |
| `/api/briefing/jobs/:id` | `GET` | Worker/job | Session or access key | Stable | Worker runtime | Poll briefing job status. |
| `/api/briefing/schedule` | `GET, POST` | Authenticated UI/internal | Session or access key | Preview | Briefings | Inspect or update briefing cadence preferences. |
| `/api/calibration` | `GET` | Preview/internal | Session or access key | Preview | Calibration | Load calibration and evaluation summaries. |
| `/api/commitments` | `GET` | Authenticated UI/internal | Session or access key | Stable | Commitments | Page through the operator commitment inbox. |
| `/api/commitments/:id` | `PATCH` | Authenticated UI/internal | Session or access key | Stable | Commitments | Complete, dismiss, or reopen one commitment. |
| `/api/context/packets` | `GET, POST` | Authenticated UI/internal | Session or access key | Preview | Context packets | List or create bounded context packets. |
| `/api/dashboard/activity` | `GET` | Authenticated UI/internal | Session or access key | Preview | Dashboard | Load dashboard activity slices. |
| `/api/dashboard/approvals` | `GET` | Authenticated UI/internal | Session or access key | Preview | Dashboard | Load dashboard approval slices. |
| `/api/dashboard/artifacts` | `GET` | Authenticated UI/internal | Session or access key | Preview | Dashboard | Load dashboard artifact slices. |
| `/api/dashboard/commitments` | `GET` | Authenticated UI/internal | Session or access key | Preview | Dashboard | Load dashboard commitment slices. |
| `/api/dashboard/core-loop` | `POST` | Authenticated UI/internal | Session or access key | Preview | Dashboard | Load or refresh core-loop telemetry. |
| `/api/dashboard/events` | `GET` | Authenticated UI/internal | Session or access key | Preview | Dashboard | Stream dashboard event updates. |
| `/api/dashboard/jobs` | `GET` | Authenticated UI/internal | Session or access key | Preview | Dashboard | Load dashboard job slices. |
| `/api/dashboard/memories` | `GET` | Authenticated UI/internal | Session or access key | Preview | Dashboard | Load dashboard memory slices. |
| `/api/dashboard/recommendations` | `GET` | Authenticated UI/internal | Session or access key | Preview | Dashboard | Load workflow recommendation summaries. |
| `/api/dashboard/recommendations/feedback` | `POST` | Authenticated UI/internal | Session or access key | Preview | Dashboard | Record recommendation feedback from the dashboard. |
| `/api/dashboard/summary` | `GET` | Authenticated UI/internal | Session or access key | Preview | Dashboard | Load the primary dashboard snapshot summary. |

Dashboard collection routes accept only their documented `limit`, `cursor`, `q`, `sort`, and route-specific filter keys. Page sizes are capped, cursors are sort-compatible, and repository page assembly uses bounded paged reads rather than direct full-collection reads for goals, approvals, memories, activity, artifacts, and jobs.

| `/api/docs/jobs/:id` | `GET` | Worker/job | Session or access key | Stable | Docs runtime | Poll document render job status. |
| `/api/docs/render` | `POST` | Authenticated UI/internal | Session or access key | Stable | Docs runtime | Enqueue reproducible document rendering. |
| `/api/github/issues/app/sync` | `POST` | Integration boundary | Bearer sync secret | Stable | GitHub issue intake | Run GitHub App pull sync for allowlisted open issues and queue governed issue-intake jobs. |
| `/api/github/issues/webhook` | `POST` | Integration boundary | GitHub webhook signature | Stable | GitHub issue intake | Accept signed GitHub issue events for governed sync. |
| `/api/goals` | `GET, POST` | Authenticated UI/internal | Session or access key | Stable | Goals and orchestration | Load dashboard data or enqueue goal creation. |
| `/api/goals/:id` | `GET` | Authenticated UI/internal | Session or access key | Stable | Goals and orchestration | Fetch one goal bundle with workflow state. |
| `/api/goals/:id/recommendations/feedback` | `POST` | Authenticated UI/internal | Session or access key | Preview | Learning flywheel | Record recommendation feedback for one goal. |
| `/api/goals/:id/refine` | `POST` | Authenticated UI/internal | Session or access key | Stable | Goals and worker runtime | Enqueue refinement for one goal. |
| `/api/goals/:id/share` | `POST, DELETE` | Authenticated UI/internal | Session or access key | Stable | Goal sharing | Create or revoke a bounded public share link. |
| `/api/goals/jobs/:id` | `GET` | Worker/job | Session or access key | Stable | Worker runtime | Poll goal creation or refinement job status. |
| `/api/governance` | `GET, POST` | Admin/governance | Session or access key | Stable | Governance | Inspect or update workspace governance policy. |
| `/api/governance/audit` | `GET` | Admin/governance | Session or access key | Stable | Governance | Export governance and audit evidence. |
| `/api/governance/privacy` | `GET, POST` | Admin/governance | Session or access key | Stable | Privacy operations | Inspect privacy controls or queue retention, export, and deletion work. |
| `/api/governance/simulate` | `POST` | Admin/governance | Session or access key | Stable | Governance | Run policy simulation against proposed governance settings. |
| `/api/health` | `GET` | Public operational | None | Stable | Operations | Return process liveness. |
| `/api/integrations` | `GET, POST` | Authenticated UI/internal | Session or access key | Stable | Integrations | Inspect or update adapter readiness and provider state. |
| `/api/integrations/google/callback` | `GET` | Integration boundary | OAuth state and provider response | Stable | Google integrations | Complete Google OAuth callback handling. |
| `/api/integrations/google/connect` | `GET` | Integration boundary | Session or access key | Stable | Google integrations | Start Google OAuth or return setup-required JSON. |
| `/api/integrations/local-notes` | `GET, POST` | Authenticated UI/internal | Session or access key plus production local-notes gate | Stable | Local notes | Search or create local Markdown notes without exposing filesystem paths. |
| `/api/integrations/local-notes/:slug` | `GET, PUT` | Authenticated UI/internal | Session or access key plus production local-notes gate | Stable | Local notes | Read or update one local Markdown note without exposing filesystem paths. |
| `/api/jobs/:id` | `GET` | Worker/job | Session or access key | Stable | Worker runtime | Poll a durable job through the generic job surface. |
| `/api/jobs/:id/events` | `GET` | Worker/job | Session or access key | Preview | Worker runtime | Stream one durable job event feed. |
| `/api/jobs/:id/replay` | `POST` | Admin/governance | Session or access key | Stable | Worker runtime | Replay a permitted dead-letter job through the governed mutation boundary. |
| `/api/memory` | `GET, POST` | Authenticated UI/internal | Session or access key | Stable | Memory | Review or add memory records. |
| `/api/memory/:id` | `PATCH` | Authenticated UI/internal | Session or access key | Stable | Memory | Review, confirm, or update one memory record. |
| `/api/memory/recommendations` | `GET` | Authenticated UI/internal | Session or access key | Preview | Learning flywheel | Load memory-backed workflow recommendations. |
| `/api/nl/intent` | `GET, POST` | Authenticated UI/internal | Session or access key | Preview | Natural language router | Inspect NL capability metadata or execute one bounded intent. |
| `/api/operations/recovery` | `POST` | Admin/governance | Session or access key | Preview | Operations tower | Run a bounded recovery or remediation action. |
| `/api/operator-products` | `GET, POST` | Authenticated UI/internal | Session or access key | Preview | Operator products | List or select an operator product pack. |
| `/api/provenance/graph` | `GET` | Authenticated UI/internal | Session or access key | Preview | Provenance | Load the provenance graph. |
| `/api/ready` | `GET` | Public operational | None | Stable | Operations | Return a public-safe readiness summary and detailed-report pointer. |
| `/api/ready/details` | `GET` | Authenticated UI/internal | Session or access key | Stable | Operations | Return detailed readiness across auth, storage, queue, and connectors. |
| `/api/session` | `POST, DELETE` | Public operational | Access key for POST; session for DELETE | Stable | Auth | Create or clear the dashboard session. |
| `/api/share/view` | `POST` | Public signed | Signed share token | Stable | Goal sharing | Read a public shared-goal projection. |
| `/api/slack/notify` | `POST` | Integration boundary | Session or access key | Preview | Slack | Send an approval-safe Slack notification. |
| `/api/slack/webhook` | `POST` | Integration boundary | Slack signature when configured | Preview | Slack | Accept Slack events for governed processing. |
| `/api/telegram/notify` | `POST` | Integration boundary | Session or access key | Preview | Telegram | Send an approval-safe Telegram notification. |
| `/api/telegram/webhook` | `POST` | Integration boundary | Telegram secret token when configured | Preview | Telegram | Accept Telegram events for governed processing. |
| `/api/templates` | `GET, POST` | Authenticated UI/internal | Session or access key | Stable | Templates | List or create reusable goal templates. |
| `/api/templates/:id` | `PATCH, DELETE` | Authenticated UI/internal | Session or access key | Stable | Templates | Update or delete one reusable goal template. |
| `/api/templates/:id/run` | `POST` | Authenticated UI/internal | Session or access key | Stable | Templates and worker runtime | Enqueue a template run. |
| `/api/templates/jobs/:id` | `GET` | Worker/job | Session or access key | Stable | Worker runtime | Poll template-run job status. |
| `/api/watchers` | `GET, POST` | Authenticated UI/internal | Session or access key | Stable | Watchers | List or create workflow watchers. |
| `/api/watchers/:id` | `PATCH` | Authenticated UI/internal | Session or access key | Stable | Watchers | Pause or resume a watcher. |
| `/api/workflow-templates` | `GET, POST` | Authenticated UI/internal | Session or access key | Preview | Workflow templates | List or create workflow templates. |
| `/api/workflow-templates/:id` | `GET, PUT, DELETE` | Authenticated UI/internal | Session or access key | Preview | Workflow templates | Inspect, update, or delete one workflow template. |
| `/api/workspaces` | `GET, POST` | Admin/governance | Session or access key | Stable | Workspaces | List, create, or select workspaces and members. |

## Mutating Route Governance Matrix

Every route with `POST`, `PUT`, `PATCH`, or `DELETE` must declare its mutation posture here. This matrix is the acceptance contract for auth, owner/workspace scope, idempotency, rate limiting, stale-write protection, and audit behavior. `Not applicable` is allowed only when the route does not persist or mutate durable records for that column.

| Endpoint | Mutating methods | Auth boundary | Owner/workspace scope | Idempotency posture | Rate limiting | Stale-write posture | Audit/side-effect behavior |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `/api/agents` | `POST` | Session or access key | Active user workspace scope for custom agent definitions. | Creation payload is deterministic by agent id or generated id. | Session/access-key abuse limit at route boundary. | Create-only, no stale record precondition. | Agent definition is persisted through repository state. |
| `/api/agents/:id` | `PUT`, `DELETE` | Session or access key | Agent id is resolved under the authenticated user workspace. | Route target id is the idempotency boundary. | Session/access-key abuse limit at route boundary. | Whole-record update/delete uses the current route id contract. | Agent update/delete is persisted through repository state. |
| `/api/agents/:id/clone` | `POST` | Session or access key | Source and clone remain under the authenticated user workspace. | Clone creates a new definition and must not mutate the source. | Session/access-key abuse limit at route boundary. | Create-only, no stale record precondition. | Clone output is persisted through repository state. |
| `/api/agents/:id/memories` | `POST` | Session or access key | Agent-scoped memory is resolved under the authenticated user workspace. | Memory id or repository-generated id is the idempotency boundary. | Session/access-key abuse limit at route boundary. | Create-only, no stale record precondition. | Memory creation is persisted through repository state. |
| `/api/agents/import` | `POST` | Session or access key | Imported agent definitions are owned by the authenticated user workspace. | Imported definition identity is normalized before persistence. | Session/access-key abuse limit at route boundary. | Create-only, no stale record precondition. | Import is persisted through repository state. |
| `/api/approvals/:id/respond` | `POST` | Session or access key | Approval id is resolved under the authenticated user and workspace context. | Approval response is keyed by approval id and response action. | Session/access-key abuse limit at route boundary. | Approval state is checked before response mutation. | Approval decision and follow-up job are recorded durably. |
| `/api/approvals/batch/preview` | `POST` | Session or access key | Candidate approvals are scoped to the authenticated dashboard context. | Preview-only, no durable mutation. | Session/access-key abuse limit at route boundary. | Not applicable for preview-only evaluation. | No external side effect. |
| `/api/approvals/batch/respond` | `POST` | Session or access key | Approval ids are resolved under the authenticated user and workspace context. | Batch operation is bounded by approval ids and decision payload. | Session/access-key abuse limit at route boundary. | Each approval state is checked before mutation. | Approval decisions and follow-up jobs are recorded durably. |
| `/api/autopilot/events` | `POST` | Session or access key | Event source is constrained by authenticated workspace context. | Deduplication key is required by autopilot event contract. | Route-specific abuse limit. | Queue append only, stale-write protection handled by worker claim. | Queued event and worker job evidence are persisted. |
| `/api/autopilot/settings` | `POST` | Session or access key | Settings apply to the active authenticated workspace. | Settings update is keyed by workspace. | Route-specific abuse limit. | Last-write wins within authenticated workspace settings. | Settings update is persisted through repository state. |
| `/api/briefing` | `POST` | Session or access key | Briefing request is scoped to authenticated dashboard context. | Queued job id is the idempotency boundary. | Session/access-key abuse limit at route boundary. | Queue append only, stale-write protection handled by worker claim. | Briefing job is recorded durably. |
| `/api/briefing/schedule` | `POST` | Session or access key | Schedule preferences apply to authenticated dashboard context. | Schedule update is keyed by workspace/user preference. | Session/access-key abuse limit at route boundary. | Last-write wins for schedule preferences. | Preference update is persisted through repository state. |
| `/api/commitments/:id` | `PATCH` | Session or access key | Commitment id is resolved under authenticated user/workspace scope. | Route target id and requested status are the idempotency boundary. | Session/access-key abuse limit at route boundary. | Requires concrete `If-Match` updatedAt precondition. | Commitment state transition is persisted through repository state. |
| `/api/context/packets` | `POST` | Session or access key | Context packet belongs to authenticated user/workspace scope. | Packet id or repository-generated id is the idempotency boundary. | Session/access-key abuse limit at route boundary. | Create-only, no stale record precondition. | Context packet is persisted through repository state. |
| `/api/dashboard/core-loop` | `POST` | Session or access key | Core-loop telemetry request is scoped to authenticated dashboard context. | Refresh operation is bounded by request payload. | Session/access-key abuse limit at route boundary. | Not applicable for read/refresh payload assembly. | No external side effect. |
| `/api/dashboard/recommendations/feedback` | `POST` | Session or access key | Feedback is scoped to authenticated dashboard recommendation context. | Feedback target id is the idempotency boundary. | Session/access-key abuse limit at route boundary. | Append/update feedback only, no stale record precondition. | Feedback is persisted for learning/recommendation evidence. |
| `/api/docs/render` | `POST` | Session or access key | Render request is scoped to authenticated operator context. | Queued docs job id is the idempotency boundary. | Session/access-key abuse limit at route boundary. | Queue append only, stale-write protection handled by worker claim. | Docs render job and output evidence are persisted. |
| `/api/github/issues/app/sync` | `POST` | Bearer sync secret | Repository allowlist constrains synced issue source. | GitHub issue identity and sync window dedupe queued work. | Shared sync-secret abuse guard. | Sync is append/queue oriented; stale issue data is refreshed from GitHub. | Issue-intake jobs are persisted with sanitized evidence. |
| `/api/github/issues/webhook` | `POST` | GitHub webhook signature | Repository allowlist and event payload constrain source. | GitHub delivery/event identity dedupes queued work. | Signature and route abuse guard. | Webhook events are append/queue oriented. | Issue-intake jobs are persisted with sanitized evidence. |
| `/api/goals` | `POST` | Session or access key | Goal creation is scoped to authenticated user/workspace context. | Queued goal job id is the idempotency boundary. | Session/access-key abuse limit at route boundary. | Queue append only, stale-write protection handled by worker claim. | Goal job is persisted with worker evidence. |
| `/api/goals/:id/recommendations/feedback` | `POST` | Session or access key | Goal id and feedback target are resolved under authenticated user/workspace scope. | Goal id plus recommendation id is the idempotency boundary. | Session/access-key abuse limit at route boundary. | Append/update feedback only, no stale record precondition. | Feedback is persisted for learning/recommendation evidence. |
| `/api/goals/:id/refine` | `POST` | Session or access key | Goal id is resolved under authenticated user/workspace scope. | Queued refinement job id is the idempotency boundary. | Session/access-key abuse limit at route boundary. | Worker checks current goal state before applying output. | Refinement job and artifacts are persisted. |
| `/api/goals/:id/share` | `POST`, `DELETE` | Session or access key | Goal id is resolved under authenticated user/workspace scope and sharing governance. | Share token or goal id is the idempotency boundary. | Session/access-key abuse limit at route boundary. | Share revoke/create checks current share state. | Share creation/revocation is recorded in audit state. |
| `/api/governance` | `POST` | Session or access key | Only the active workspace owner can update governance. | Workspace governance record is the idempotency boundary. | Route-specific abuse limit. | Last-write wins after owner check. | Governance update is persisted and returned with conformance evidence. |
| `/api/governance/privacy` | `POST` | Session or access key | Privacy operation is scoped to active workspace governance and owner permissions. | Operation id and operation type dedupe privacy work. | Route-specific abuse limit. | Queue append only, stale-write protection handled by worker claim. | Privacy operation and audit evidence are persisted. |
| `/api/governance/simulate` | `POST` | Session or access key | Simulation is scoped to active workspace context. | Simulation-only, no durable mutation. | Route-specific abuse limit. | Not applicable for simulation-only evaluation. | No external side effect. |
| `/api/integrations` | `POST` | Session or access key | Provider readiness/state update is scoped to authenticated user/workspace. | Provider id is the idempotency boundary. | Session/access-key abuse limit at route boundary. | Last-write wins for provider readiness state. | Integration state is persisted through repository state. |
| `/api/integrations/local-notes` | `POST` | Session or access key; production additionally requires `AGENTIC_LOCAL_NOTES_ENABLED`, `AGENTIC_NOTES_PATH`, and `AGENTIC_LOCAL_NOTES_ALLOWED_ROOT`. | Local note creation is scoped to authenticated operator context and a configured filesystem root. | Slug is canonicalized as the idempotency boundary; absolute paths are never returned. | Session/access-key abuse limit at route boundary. | Create path is canonicalized and checked against the allowed root before write. | Local note file write is the side effect. |
| `/api/integrations/local-notes/:slug` | `PUT` | Session or access key; production additionally requires `AGENTIC_LOCAL_NOTES_ENABLED`, `AGENTIC_NOTES_PATH`, and `AGENTIC_LOCAL_NOTES_ALLOWED_ROOT`. | Slug is canonicalized inside the local notes boundary and scoped to the configured filesystem root. | Slug is the idempotency boundary; absolute paths are never returned. | Session/access-key abuse limit at route boundary. | Last-write wins for the canonical local note slug after root-scope validation. | Local note file write is the side effect. |
| `/api/jobs/:id/replay` | `POST` | Session or access key | Job id is resolved under authenticated operator context. | Replay request is keyed by source job id. | Route-specific abuse limit. | Replay only permitted for eligible dead-letter state. | Replay job and recovery evidence are persisted. |
| `/api/memory` | `POST` | Session or access key | Memory creation is scoped to authenticated user/workspace context. | Memory id or generated id is the idempotency boundary. | Session/access-key abuse limit at route boundary. | Create-only, no stale record precondition. | Memory record is persisted. |
| `/api/memory/:id` | `PATCH` | Session or access key | Memory id is resolved under authenticated user/workspace context. | Memory id plus review action is the idempotency boundary. | Session/access-key abuse limit at route boundary. | Current memory state is checked before review mutation. | Memory review/update is persisted. |
| `/api/nl/intent` | `POST` | Session or access key | Intent execution is scoped to authenticated dashboard capabilities. | Intent payload and bounded action target are the idempotency boundary. | Session/access-key abuse limit at route boundary. | Stale-write posture is delegated to the selected bounded action. | Intent output or queued action evidence is persisted when applicable. |
| `/api/operations/recovery` | `POST` | Session or access key | Recovery action is scoped to authenticated operator and allowed recovery target. | Recovery target id and action type dedupe work. | Route-specific abuse limit. | Recovery validates current target state before mutation. | Recovery action and evidence are recorded. |
| `/api/operator-products` | `POST` | Session or access key | Product pack selection is scoped to authenticated dashboard context. | Selected product id is the idempotency boundary. | Session/access-key abuse limit at route boundary. | Last-write wins for selected operator product. | Selection state is persisted through repository state. |
| `/api/session` | `POST`, `DELETE` | Access key for `POST`; session for `DELETE`. | Session state is scoped to the authenticated browser/session. | Session token is the idempotency boundary. | Auth/session rate limit. | Not applicable for session bootstrap/clear. | Session creation/revocation updates auth runtime state. |
| `/api/share/view` | `POST` | Signed share token | Token scope constrains the shared goal projection. | View recording is keyed by token and request class. | Public share view abuse guard. | Not applicable for signed read projection. | Share view audit is recorded without exposing private state. |
| `/api/slack/notify` | `POST` | Session or access key | Notification target is constrained by configured Slack adapter readiness. | Notification payload target is the idempotency boundary. | Session/access-key abuse limit at route boundary. | Not applicable for outbound notification send. | External Slack send is approval-safe and logged by adapter path. |
| `/api/slack/webhook` | `POST` | Slack signature when configured | Slack team/channel/action payload constrains source. | Slack action id or event id dedupes processing. | Signature and route abuse guard. | Current approval state is checked before mutation. | Approval response or queued work is persisted. |
| `/api/telegram/notify` | `POST` | Session or access key | Notification target is constrained by configured Telegram adapter readiness. | Notification payload target is the idempotency boundary. | Session/access-key abuse limit at route boundary. | Not applicable for outbound notification send. | External Telegram send is approval-safe and logged by adapter path. |
| `/api/telegram/webhook` | `POST` | Telegram secret token when configured | Telegram chat/action payload constrains source. | Telegram update id dedupes processing. | Secret-token and route abuse guard. | Current approval state is checked before mutation. | Approval response or queued work is persisted. |
| `/api/templates` | `POST` | Session or access key | Template creation is scoped to authenticated user/workspace context. | Template id or generated id is the idempotency boundary. | Session/access-key abuse limit at route boundary. | Create-only, no stale record precondition. | Template is persisted through repository state. |
| `/api/templates/:id` | `PATCH`, `DELETE` | Session or access key | Template id is resolved under authenticated user/workspace context. | Template id plus mutation action is the idempotency boundary. | Session/access-key abuse limit at route boundary. | Requires concrete `If-Match` updatedAt precondition. | Template update/delete is persisted through repository state. |
| `/api/templates/:id/run` | `POST` | Session or access key | Template id is resolved under authenticated user/workspace context. | Queued template-run job id is the idempotency boundary. | Session/access-key abuse limit at route boundary. | Worker checks current template state before applying output. | Template-run job and evidence are persisted. |
| `/api/watchers` | `POST` | Session or access key | Watcher creation is scoped to authenticated user/workspace context. | Watcher id or generated id is the idempotency boundary. | Session/access-key abuse limit at route boundary. | Create-only, no stale record precondition. | Watcher registration is persisted. |
| `/api/watchers/:id` | `PATCH` | Session or access key | Watcher id is resolved under authenticated user/workspace context. | Watcher id plus requested state is the idempotency boundary. | Session/access-key abuse limit at route boundary. | Current watcher state is checked before pause/resume. | Watcher state transition is persisted. |
| `/api/workflow-templates` | `POST` | Session or access key | Workflow template creation is scoped to authenticated user/workspace context. | Template id or generated id is the idempotency boundary. | Session/access-key abuse limit at route boundary. | Create-only, no stale record precondition. | Workflow template is persisted through repository state. |
| `/api/workflow-templates/:id` | `PUT`, `DELETE` | Session or access key | Workflow template id is resolved under authenticated user/workspace context. | Template id plus mutation action is the idempotency boundary. | Session/access-key abuse limit at route boundary. | Requires concrete `If-Match` updatedAt precondition. | Workflow template update/delete is persisted through repository state. |
| `/api/workspaces` | `POST` | Session or access key | Workspace creation/selection is scoped to authenticated user ownership and membership. | Workspace id or slug is the idempotency boundary. | Session/access-key abuse limit at route boundary. | Create/select path validates current membership state. | Workspace, membership, and default governance records are persisted. |
